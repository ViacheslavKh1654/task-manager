import s from "./footer.module.css"

export const Footer = () => {

    return (
        <footer className={s.footer}>
            <div className="inner">
                <h2>Footer</h2>
            </div>
        </footer>

    )
}