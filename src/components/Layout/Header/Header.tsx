import s from "./header.module.css"

export const Header = () => {

    return (
        <header className={s.header}>
            <div className="inner">
                <h1>Header</h1>
            </div>
        </header>
    )
}